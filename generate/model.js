/*
    ${UMODEL}.js - ${UMODEL} model (esp-vue-skeleton)
 */
import Rest from 'js-rest'

export default new Rest('${MODEL}', {
    /*
        Additional restful endpoints here

        mappings: { method: 'POST', uri: ':controller/mappings' },
     */
})
